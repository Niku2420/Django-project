To Run the code we need to have pip updated
secondly Django setup 
installing of sudo apt-get install libpq-dev
pip install psycopg2-binary
pip install django-heroku

and then to run website python3 manage.py runserver
